import { PagePlaceholder } from './PagePlaceholder';

export function IncomesPage() {
  return (
    <PagePlaceholder
      title="Ingresos"
      description="Pantalla para registrar y listar ingresos por fecha, monto y categoría."
    />
  );
}
