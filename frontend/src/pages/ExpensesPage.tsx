import { PagePlaceholder } from './PagePlaceholder';

export function ExpensesPage() {
  return (
    <PagePlaceholder
      title="Gastos"
      description="Pantalla para registrar y listar gastos con filtros por período y categoría."
    />
  );
}
